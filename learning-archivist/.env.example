# Anthropic API (필수)
ANTHROPIC_API_KEY=sk-ant-...
ANALYSIS_MODEL=claude-opus-4-8

# Whisper (로컬 STT)
WHISPER_MODEL=large-v3
WHISPER_DEVICE=auto       # cpu | cuda | auto  (Apple Silicon은 cpu로 동작)
WHISPER_COMPUTE=int8      # int8(가벼움) | float16 | float32

# 워처 폴링 주기(초)
POLL_INTERVAL=300
